# from parse import nnp
#
# def add_nnp(pair):
#     nnp.add(pair[0])
#

# def conjunction_handler(prev, cur, extra):
#     global graph_edge_labels, graph_edges, graph_nodes
#     prev.prev.next.append(cur)
#     prev.prev.edge.append(prev.prev.edge[-1])
#     graph_edges.append([prev.prev.label, cur.label])
#     graph_edge_labels[(prev.prev.label, cur.label)] = extra